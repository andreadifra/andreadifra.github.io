# The renv sandbox hangs on this Windows/rig setup while constructing the
# system-library sandbox. Keep renv active, but disable only that sandbox layer.
options(renv.config.sandbox.enabled = FALSE)

source("renv/activate.R")
